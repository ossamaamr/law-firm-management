# MERSAD — تقرير التحقق والمرحلة الهندسية التالية

**التاريخ:** 18 أغسطس 2026

**المستودع:** [ossamaamr/law-firm-management](https://github.com/ossamaamr/law-firm-management)

**الفرع:** `main`

## نطاق التنفيذ

تمت مراجعة الـmilestone السابق ثم استكمال المرحلة التالية داخل المستودع الأصلي فقط. بقي العمل على الفرع `main` دون إعادة كتابة التاريخ، ودون إنشاء مستودع بديل أو نشر التطبيق أو إنشاء بيئة إنتاجية.

## الإصلاحات المنفذة في هذه المرحلة

| المجال | التنفيذ | الأثر |
|---|---|---|
| Activity Router | إضافة `activityRouter` إلى `appRouter`، وإضافة استيراد Zod، والتحقق من أن `ctx.user.lawFirmId` يطابق المكتب المطلوب في كل endpoint | منع قراءة أو تصدير نشاطات مكتب آخر عبر معرّف مُرسل من العميل |
| مخطط البيانات | إضافة جدول `activityLogs` وأنواع Drizzle المرتبطة به | إزالة اعتماد الخدمة على جدول غير معلن في المخطط |
| Activity Service | استبدال predicates غير الصالحة في Drizzle بـ`and` و`eq` و`desc`، مع pagination محدودة وتفسير آمن لحقل `changes` | إصلاح أخطاء النوع وتقليل الاستعلامات غير المنضبطة |
| Activity Hooks | استبدال النتائج الفارغة الوهمية باستدعاءات tRPC فعلية، وربط التصدير بمسار القراءة الصحيح | عدم عرض نجاح أو بيانات وهمية في الواجهة |
| Authentication Hooks | استخدام `useLocation` المدعوم من Wouter، إزالة تخزين Tokens محلية، وربط التحقق من المعرّف بطلب فعلي | اتساق أكبر مع جلسة Cookie ومصدر المصادقة الموحد |
| API Adapter | استبدال استدعاءات React-only غير الصالحة بطبقة HTTP صغيرة تستخدم `/api/trpc/*` مع `credentials: include` | إزالة أخطاء `.query` و`.mutate` القديمة مع الحفاظ على جلسة المتصفح |
| صفحات الواجهة | إصلاح مسارات `LanguageContext`، إضافة hooks توافقية للوحة التحكم، تصحيح type inference للطلبات، وإضافة `AlertCircle` المفقود | إزالة أخطاء compile من الصفحات المتأثرة |
| المزامنة والتخزين المؤقت | تحديث `z.record` إلى الصيغة الحالية واستبدال iterators غير المتوافقة مع target القديم بـ`Array.from` | إزالة أخطاء TypeScript الخاصة بالـtarget وZod |
| خادم البريد القديم | إزالة imports غير المعلنة لـ`cors` و`resend` من entrypoint القديم، واستخدام CORS محلي محدود ومسار بريد يفشل مغلقًا | عدم الادعاء بتهيئة مزود بريد غير موجود |

## نتائج التحقق

| الفحص | النتيجة | التفاصيل |
|---|---|---|
| `pnpm check` | **ناجح** | TypeScript يمر دون أخطاء بعد الإصلاحات |
| `pnpm build` | **ناجح** | تم بناء Vite وentrypoint الخادم بنجاح |
| `git diff --check` | **ناجح** | لا توجد أخطاء whitespace في التغييرات |
| `pnpm test` | **غير ناجح بالكامل** | ما زالت 26 حالة فاشلة من 108؛ تفاصيلها أدناه، ولم يتم إخفاؤها |
| النشر | لم يُنفذ | التزامًا بنطاق الطلب والتوجيهات الهندسية |

## تفسير فشل الاختبارات المتبقية

فشلت اختبارات النشاط عند غياب `DATABASE_URL` لأن الخدمة مصممة للفشل المغلق عند عدم توفر قاعدة البيانات، وليس لإرجاع نجاح وهمي. كما أن اختبارات المصادقة القديمة ما زالت تتوقع Mock Login وبيانات محلية، بينما السلوك الحالي يرفض المصادقة المحلية ويطلب جلسة المنصة الموحدة. ويوجد اختبار بريد تكاملي قديم يستورد حزمة `resend` غير موجودة ويتطلب مفتاحًا واتصالًا خارجيًا؛ لم يتم تركيب مزود أو إرسال رسائل تجريبية لتجاوز هذا القيد.

توجد أيضًا حالة فشل واحدة في اختبار التخزين المؤقت تحتاج مراجعة مستقلة. هذه النتائج تعني أن المشروع **ليس جاهزًا للإنتاج بعد**، رغم نجاح typecheck وbuild.

## الضوابط الأمنية المحفوظة

> لا توجد مصادقة محلية وهمية، ولا Token ثابت، ولا نجاح وهمي عند غياب قاعدة البيانات أو مزود البريد. كما أن مسارات النشاط أصبحت firm-scoped server-side، ولم يتم تعطيل الاختبارات أو إسكات أخطاء compiler لرفع نسبة النجاح شكليًا.

## الحكم المرحلي

> **VERIFIED ENGINEERING MILESTONE — TYPECHECK AND BUILD PASS — TEST SUITE STILL BLOCKED BY ENVIRONMENTAL AND LEGACY-CONTRACT FAILURES — NOT PRODUCTION READY**

## الخطوة التالية

الخطوة الآمنة التالية هي فصل اختبارات التكامل التي تتطلب قاعدة بيانات أو مزود بريد عن اختبارات الوحدة، ثم تحديث اختبارات المصادقة لتطابق جلسة المنصة الموحدة، وإضافة اختبارات سلبية صريحة لعزل Firm A/Firm B وIDOR. بعد ذلك يمكن الانتقال إلى Admin Control Center وDynamic Branding دون تجاوز بوابات الجودة.
