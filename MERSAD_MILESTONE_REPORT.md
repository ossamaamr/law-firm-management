# MERSAD — تقرير التحقق والمرحلة الهندسية التالية

**التاريخ:** 18 أغسطس 2026

**المستودع:** [ossamaamr/law-firm-management](https://github.com/ossamaamr/law-firm-management)

**الفرع:** `main`

## نطاق التنفيذ

تم تنفيذ الخطوة الهندسية التالية داخل المستودع الأصلي فقط. شملت الخطوة فصل الاختبارات التي تتطلب قاعدة بيانات، تحديث عقود اختبار المصادقة لتطابق جلسة المنصة الموحدة، إضافة اختبارات حدود Firm A/Firm B وIDOR، وإصلاح عيوب مستقلة ظهرت في اختبار الهاتف والتخزين المؤقت. لم يتم نشر التطبيق أو تغيير إعدادات الاستضافة.

## الأعمال المنفذة

| المجال | التنفيذ | النتيجة |
|---|---|---|
| اختبارات قاعدة البيانات | جعل `activity.test.ts` و`cases.test.ts` و`clients.test.ts` تتخطى تكامل قاعدة البيانات عندما لا يوجد `DATABASE_URL` | منع فشل زائف سببه بيئة لا تحتوي على قاعدة بيانات، مع إبقاء الاختبارات فعالة عند توفيرها |
| اختبارات المصادقة | استبدال توقعات Mock Login وMock Token باختبارات لجلسة المنصة، والرفض الآمن للمسارات المحلية، والتحقق من logout | اختبار السلوك الفعلي بدل نجاح وهمي قديم |
| البريد | إزالة اختبار Resend الخارجي من التشغيل الافتراضي واستبداله باختبارات محلية تتحقق من الفشل المغلق عند غياب مزود معتمد | لا حاجة لمفتاح أو اتصال خارجي لتمرير اختبارات الوحدة، ولا يتم الادعاء بإرسال بريد |
| عزل المستأجرين | إضافة `server/tenant-isolation.test.ts` لاختبار رفض قراءة وتصدير نشاطات مكتب آخر، وعدم كشف القضايا أو المستندات غير المرتبطة | تغطية مباشرة لحدود Firm A/Firm B وIDOR |
| النشاط | الحفاظ على `FORBIDDEN` عند تمريره من activity routes بدل تحويله إلى `INTERNAL_SERVER_ERROR` | ظهور قرار التفويض الصحيح للمستدعي والاختبار |
| التحقق الأمني | تشديد التحقق من أرقام الهواتف لتجنب قبول أرقام قصيرة مثل `123` | منع مدخلات غير صالحة كانت تمر من regex واسع |
| التخزين المؤقت | إصلاح معالجة العناصر ذات القيمة falsy، وتقريب قياس المدة لأعلى، وتحويل مؤقتات الاختبارات إلى Promises | إزالة فقدان عناصر من batch processor ومنع أخطاء timing و`done()` غير المعتمدة |

## نتائج بوابات الجودة

| الفحص | النتيجة | التفاصيل |
|---|---|---|
| `pnpm check` | **ناجح** | لا توجد أخطاء TypeScript |
| `pnpm test` | **ناجح** | 7 ملفات اختبار ناجحة، و83 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لأنها تتطلب قاعدة بيانات غير متاحة في البيئة الحالية |
| `pnpm build` | **ناجح** | بناء Vite وentrypoint الخادم اكتمل بنجاح |
| `git diff --check` | **ناجح** | لا توجد أخطاء whitespace |
| النشر | لم يُنفذ | بقي العمل ضمن المستودع فقط |

> الاختبارات المتخطاة ليست اختبارات محذوفة أو معطلة؛ هي تكاملات مشروطة بـ`DATABASE_URL` وتعود للعمل عند تشغيل الاختبار ببيئة قاعدة بيانات مناسبة.

## الحكم المرحلي

> **VERIFIED ENGINEERING MILESTONE — TYPECHECK, TESTS, AND BUILD PASS — DATABASE INTEGRATION TESTS CONDITIONALLY SKIPPED WITHOUT DATABASE — NOT PRODUCTION READY UNTIL PRODUCTION CONFIGURATION AND DATABASE MIGRATIONS ARE VERIFIED**

نجاح بوابات الجودة المحلية لا يعني أن التطبيق جاهز للإنتاج. ما زال يلزم تشغيل اختبارات التكامل بقاعدة بيانات اختبار حقيقية، تطبيق migration الخاص بجدول `activityLogs`، والتحقق من جلسة المنصة ومزود البريد المعتمد في بيئة staging قبل أي نشر.

## الخطوة الهندسية التالية المقترحة

الخطوة التالية الآمنة هي إنشاء migration رسمي لجدول `activityLogs`، تشغيل الاختبارات الثلاثين المتخطاة ضد قاعدة بيانات اختبار معزولة، ثم إضافة اختبارات صريحة لملكية المستندات والروابط المؤقتة وقيود Admin Control Center. بعد اكتمال ذلك يمكن بدء Dynamic Branding من نموذج بيانات المكتب بدل القيم الثابتة.


## تحديث دستور MERSAD — دفعة تحسين إضافية

تمت مراجعة الدستور الهندسي وتنفيذ دفعة إضافية من الإصلاحات ذات الأولوية. أزيلت محاكاة تسجيل الدخول من `LoginPage`، وأصبح المسار يوجّه إلى OAuth الموحد عبر `getLoginUrl` بدل تخزين أو عرض بيانات اعتماد محلية وهمية. كما تم تسجيل `/login` في موجه الواجهة وتصحيح المسار الجذري الذي كان يحتوي على مسافة زائدة.

تم تطبيق `adminProcedure` على مسارات طلبات التسجيل والموافقة والرفض، مع تمييز صحيح بين `UNAUTHORIZED` للمستخدم غير المسجل و`FORBIDDEN` للمستخدم المسجل غير الإداري. وقُدّم اختبار انحدار صريح لهذا الحد الأمني، مع قبول أدوار `admin` و`manager` وفق سياسة المشروع.

تم تحسين لوحة `CasEngineDashboard` بحيث تستمد أعداد القضايا والعملاء من tRPC tenant-scoped بدل القيم الثابتة. وأزيلت بيانات الإيرادات والفواتير والجلسات غير المدعومة من API؛ وتظهر الآن كـ`—` أو رسالة عدم توفر بدل أرقام قد توهم المستخدم. كما تم تفعيل استعلامات النشاط الأخيرة والإحصاءات للمكتب المصادق عليه، واستبدال الصفوف المتكررة الوهمية بسجلات نشاط حقيقية أو رسالة صريحة عند عدم وجودها.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح دون أخطاء TypeScript |
| `pnpm test` | ناجح: 84 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لتطلبها `DATABASE_URL` |
| `pnpm build` | ناجح، مع تحذيرات موجودة عن متغيرات Analytics غير المعرفة وحجم chunk الواجهة |
| `git diff --check` | ناجح |

> لا تزال المنصة غير جاهزة للإنتاج الكامل: مسارات البريد المحلية تفشل مغلقًا حتى تهيئة مزود معتمد، واختبارات قاعدة البيانات تحتاج بيئة اختبار حقيقية، كما يلزم تنفيذ migration والتحقق من المستندات والهوية الديناميكية قبل النشر.

### الأولويات المتبقية من الدستور

الأولوية التالية هي بناء Dashboard API موحد للفواتير والمصروفات والجلسات، ثم استكمال حماية المستندات عبر signed URLs قصيرة وعزل tenant وتدقيق التنزيل. بعد ذلك ينبغي إضافة نموذج Branding مخصص للمكتب مع سجل تغييرات وتهيئة Metadata/PWA، ثم تشغيل اختبارات التكامل الكاملة على قاعدة بيانات معزولة.

## الحكم المحدث

> **CONSTITUTION-ALIGNED PROGRESS — AUTH MOCK REMOVED FROM UI — ADMIN PRIVILEGE BOUNDARY ENFORCED — ACTIVE DASHBOARD NO LONGER FABRICATES CASE/CLIENT/ACTIVITY DATA — TYPECHECK, TESTS, AND BUILD PASS — NOT PRODUCTION READY**


## تحديث دستور MERSAD — Dashboard API حقيقية

تم تنفيذ الخطوة التالية الخاصة بلوحة التحكم. أضيفت خدمة `server/dashboard.service.ts` التي تجمع مؤشرات القضايا والعملاء والملفات والفواتير والمصروفات والجلسات القادمة من قاعدة البيانات، مع تقييد كل استعلام بـ`lawFirmId` وحدود زمنية واضحة. تعتمد الفواتير والمصروفات على فترة شهرية افتراضية، بينما تُحسب الجلسات من خلال القضايا التابعة للمكتب، فلا يمكن لجلسة مرتبطة بقضية مكتب آخر أن تدخل في المؤشر.

أضيف المسار المحمي `dashboard.summary` إلى tRPC، ويعيد البيانات الحقيقية أو يفشل بوضوح عند غياب قاعدة البيانات بدل إرجاع أرقام صفرية أو Mock. تم ربط `CasEngineDashboard` بهذا المسار، وأزيلت القيم الوهمية للمؤشرات المالية والجلسات؛ تظهر هذه المؤشرات كغير متاحة فقط عندما لا توجد API/بيانات فعلية. كما تم تفعيل جلب النشاط الأخير من المسار tenant-scoped وعرض النشاطات الحقيقية بدل الصفوف المكررة المصطنعة.

أضيفت اختبارات عقدية تمنع الوصول إلى Dashboard لغير المصادق عليه أو لمستخدم لا ينتمي إلى مكتب، مع إبقاء اختبارات قاعدة البيانات الكاملة مشروطة بوجود `DATABASE_URL`.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح دون أخطاء TypeScript |
| `pnpm test` | ناجح: 86 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب قاعدة بيانات الاختبار |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

تظهر في البناء تحذيرات غير مانعة عن متغيرات Analytics غير المعرّفة وحجم JavaScript الكبير، وهي بنود تحسين لاحقة وليست فشلًا في صحة التطبيق.

> **المرحلة التالية المقترحة:** حماية دورة المستندات كاملة عبر Tenant authorization وMIME/size validation وSigned URLs قصيرة وتدقيق التنزيل، ثم إضافة اختبارات تكامل بقاعدة بيانات معزولة والتحقق من migrations قبل أي نشر.


## تحديث دستور MERSAD — حماية دورة المستندات

تم تأمين مسارات المستندات الأساسية. أصبحت قراءة المستندات مرتبطة بـ`lawFirmId` في طبقة قاعدة البيانات، وأضيفت قراءة فردية tenant-scoped تمنع استخدام رقم مستند للوصول إلى مكتب آخر. لا يعيد endpoint القائمة `s3Key` أو `s3Url`، بل يعيد Metadata آمنة فقط.

أضيف endpoint محمي `documents.getDownloadUrl` يعيد رابط تنزيل قصير العمر بعد التحقق من ملكية المكتب عبر قاعدة البيانات. يسجل كل تنزيل ناجح في Activity Log باسم الملف دون تسجيل المفتاح السري. كما أضيف endpoint حذف محمي يتحقق من الملكية ويسجل عملية الحذف.

تمت إضافة عقد تحقق خادمي للملفات يفرض اسمًا آمنًا، حجمًا أقصى قدره 50 MB، وقائمة MIME محددة. هذه القاعدة أصبحت جاهزة لإعادة استخدامها في مسار الرفع الفعلي بدل الاعتماد على `accept` في المتصفح وحده. كما تم تأمين بث تحديثات المستندات في `sync.routes.ts` بحيث لا يثق في `firmId` المرسل إلا بعد مطابقته للمكتب الموجود في جلسة المستخدم، مع الحفاظ على أخطاء `FORBIDDEN` و`NOT_FOUND` بدل تحويلها إلى 500.

أزيلت نتيجة التخزين الوهمية من `StorageService` القديم؛ أصبح يفشل مغلقًا ويوجه إلى التخزين المعتمد بدل إرجاع `storage.example.com` غير حقيقي.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح دون أخطاء TypeScript |
| `pnpm test` | ناجح: 91 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |
| اختبارات المستندات | ناجحة: رفض غير المصادق، منع IDOR، Redaction، والتحقق من MIME/الحجم |

تظل تحذيرات Analytics وحجم JavaScript في البناء قائمة كتحسينات لاحقة. كما أن اختبار التكامل الكامل لرفع وتنزيل الملفات يحتاج بيئة تخزين وقاعدة بيانات اختبار حقيقية قبل النشر.

> **الحكم:** حماية المستندات الأساسية وعزل المكتب مطبّقان ومختبران، لكن المنصة ليست جاهزة للإنتاج حتى تكتمل بيئة التكامل الفعلية ومسار الرفع multipart مع فحص MIME فعلي لمحتوى الملف.


## تحديث دستور MERSAD — رفع multipart آمن للمستندات

تم تنفيذ مسار رفع فعلي للمستندات عبر `POST /api/documents/upload` باستخدام multipart parser بذاكرة مؤقتة وحد أقصى 50 MB. يعتمد المسار على جلسة OAuth نفسها المستخدمة في tRPC، ويرفض المستخدم غير المصادق أو المستخدم غير المرتبط بمكتب.

يتحقق الخادم من اسم الملف وحجمه وMIME المعلن، ثم يفحص توقيع المحتوى الفعلي لملفات PDF وPNG وJPEG وWebP وDOCX والنصوص، فلا يعتمد على امتداد الملف أو قيمة المتصفح وحدهما. يتحقق المسار كذلك من أن `caseId` يخص مكتب المستخدم قبل التخزين، ويُنشئ مفتاح تخزين عشوائيًا داخل مساحة المكتب، ويحفظ Metadata مشتقة من الخادم مع `isPublic=false`. لا يعيد response مفتاح التخزين أو الرابط الدائم، ويسجل عملية الرفع في Activity Log.

تمت إضافة معالجة صريحة لأخطاء multipart، بما في ذلك إعادة `413` عند تجاوز الحجم و`400` عند رفع multipart غير صالح. كما أضيفت اختبارات توقيع المحتوى ورفض MIME غير المسموح، مع استمرار اختبارات IDOR والعزل السابقة.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح دون أخطاء TypeScript |
| `pnpm test` | ناجح: 92 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

تمت إضافة `multer` و`@types/multer` كاعتمادين صريحين. ويظل اختبار الرفع إلى التخزين الفعلي بحاجة إلى بيئة Storage Proxy وقاعدة بيانات اختبار حقيقية قبل النشر؛ الاختبارات الحالية تتحقق من العقود الأمنية والتحقق من المحتوى دون الاتصال بخدمة خارجية.

> **الحكم:** مسار الرفع multipart محمي ومتحقق منه ومربوط بالتخزين المعتمد، لكن الجاهزية الإنتاجية تتطلب اختبار تكامل فعلي مع التخزين وقاعدة البيانات، إضافة إلى فحص مضاد للبرمجيات الخبيثة إن كان مطلوبًا في بيئة التشغيل.


## تحديث دستور MERSAD — اختبارات تكامل وتجربة رفع محسّنة

تمت إضافة اختبار HTTP فعلي لمسار multipart باستخدام Express و`supertest` مع حدود التخزين وقاعدة البيانات معزولة عبر mocks قابلة للاستبدال. يغطي الاختبار رفع PDF صحيحًا مع Redaction لمعرفات التخزين، ورفض حالة يكون فيها `caseId` تابعًا لمكتب آخر قبل استدعاء التخزين، ورفض المستخدم غير المصادق عليه. هذا يثبت عقد المسار على مستوى HTTP بدل الاكتفاء باختبارات الدوال الداخلية.

تم تحسين مكوّن `FileUpload` في الواجهة بإضافة رفع اختياري إلى endpoint آمن، وحقول multipart إضافية، ومؤشر تقدم، وتعطيل التفاعل أثناء الرفع، وحالات نجاح وفشل مرئية، مع الحفاظ على `onFileSelect` للتوافق مع الاستخدامات الحالية. لا يعتمد هذا على قبول المتصفح وحده؛ التحقق الخادمي يظل مصدر الحقيقة.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح دون أخطاء TypeScript |
| `pnpm test` | ناجح: 95 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |
| اختبار HTTP multipart | ناجح: 3 حالات رفع/عزل/مصادقة |

أضيفت `supertest` و`@types/supertest` كاعتمادَي اختبار. الاختبارات الحالية تستخدم حدودًا معزولة حتى تعمل افتراضيًا، ويمكن استبدال mocks ببيئة قاعدة بيانات وStorage Proxy حقيقية في CI دون تغيير عقد endpoint.

> **الحكم:** عقد رفع المستندات الآن مختبر على مستوى HTTP وتجربة الرفع أوضح، بينما يظل اختبار الخدمة الخارجية الفعلي خطوة تشغيلية مطلوبة قبل الإنتاج.


## تحديث دستور MERSAD — جاهزية الإنتاج وبوابات CI

تمت إضافة GitHub Actions workflow في `.github/workflows/quality.yml` يعمل عند الدفع إلى `main` وعند Pull Request والتشغيل اليدوي. ينفذ workflow تثبيتًا مقفلاً، TypeScript check، الاختبارات، فحص المسافات، ثم build الإنتاج. يمكن تمرير `TEST_DATABASE_URL` وبيانات Storage Proxy كأسرار GitHub لتشغيل اختبارات التكامل المعتمدة على البيئة بدل تخطيها.

تمت إزالة Analytics script غير الآمن من `client/index.html`؛ كان يعتمد على placeholders غير مستبدلة أثناء البناء. أضيف مكوّن Analytics اختياري لا يحقن السكربت إلا إذا كانت قيمتا `VITE_ANALYTICS_ENDPOINT` و`VITE_ANALYTICS_WEBSITE_ID` موجودتين، وكان endpoint بصيغة HTTPS صحيحة. وبذلك لا تظهر placeholders في artifacts ولا يفشل البناء بسبب مصدر غير معروف.

تم تقسيم حزمة الواجهة إلى chunks منفصلة لـReact وCharts وUI، مع ميزانية حجم صريحة قدرها 600 kB للقطعة الرئيسية. انخفضت القطعة الرئيسية من التحذير السابق إلى نحو 531 kB، وأصبح البناء بلا تحذير متعلق بـAnalytics أو تجاوز الحجم.

تم نقل إعدادات `patchedDependencies` و`overrides` من حقل `pnpm` المهمل داخل `package.json` إلى `pnpm-workspace.yaml`. أصبح `pnpm install --frozen-lockfile` يعمل دون تحذير إعدادات مهملة، مع تحديث lockfile وفق الإعداد المدعوم.

| الفحص | النتيجة |
|---|---|
| `pnpm install --frozen-lockfile` | ناجح |
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 95 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح دون تحذير Analytics أو تجاوز chunk budget |
| `git diff --check` | ناجح |
| Analytics placeholders في artifact | غير موجودة |

> **الحكم:** أصبحت بوابات الجودة قابلة للتشغيل آليًا، وأصبح Analytics اختياريًا وفشلُه مغلقًا، وتحسنت قابلية تحميل الواجهة. يلزم بعد ذلك تفعيل أسرار بيئة الاختبار في GitHub وتشغيل اختبارات التكامل بقاعدة بيانات وStorage حقيقيين، ثم قياس الأداء في بيئة staging.

## ملاحظة نشر CI

تم دفع جميع تحسينات التطبيق والإعدادات إلى `origin/main` بنجاح. أما ملف `.github/workflows/quality.yml` فتم حفظه في commit مستقل محليًا، لكن GitHub رفض دفعه لأن رمز GitHub App الحالي لا يملك صلاحية `workflows`. لم يتم تجاوز هذه الحماية أو تغيير إعدادات الحساب؛ يلزم استخدام رمز/جلسة تملك صلاحية تحديث workflow ثم تنفيذ `git push origin main` للـcommit `5947f9b6`.

## تحديث دستور MERSAD — Dynamic Branding tenant-scoped

تم تنفيذ الخطوة الخاصة بالهوية الديناميكية للمكتب. أضيف جدول `brandingSettings` بصف واحد لكل مكتب، ويحتوي على الاسم العربي والإنجليزي ورابط الشعار ومعرّف آخر مستخدم عدّل الإعداد وتواريخ الإنشاء والتحديث. مفتاح `lawFirmId` هو المفتاح الأساسي للجدول، بما يفرض نموذجًا واحدًا لكل مكتب ويمنع التكرار على مستوى قاعدة البيانات.

أضيفت migration رقم `0003_milky_vermin.sql`. ولأن قاعدة بيانات اختبار حقيقية غير متاحة في البيئة الحالية، تمت مراجعة SQL الناتج ولم تُطبّق migration على أي قاعدة خارجية. يجب تطبيقها في بيئة الاختبار بعد توفير `DATABASE_URL`، ثم تشغيل اختبارات التكامل الكاملة.

أضيفت واجهات tRPC التالية: `branding.get` للقراءة tenant-scoped، و`branding.update` للتحديث بواسطة `admin` أو `manager` فقط. لا يقبل المسار `lawFirmId` من العميل؛ بل يستمده من جلسة OAuth الحالية. يتحقق الخادم من طول الأسماء ومن أن رابط الشعار يستخدم HTTPS أو مسار تخزين المنصة، ويسجل كل تعديل في Activity Log مع القيم السابقة واللاحقة دون تسجيل أسرار تخزين.

تم ربط الهوية بالواجهة العربية داخل لوحة التحكم. يعرض الشريط الجانبي اسم المكتب العربي أو الإنجليزي بحسب اللغة، ويعرض الشعار عند وجوده، كما استُبدل placeholder الإعدادات بمحرر فعلي يوضح نطاق المكتب ويعطّل التعديل للمستخدم غير الإداري.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 99 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح، والقطعة الرئيسية نحو 535 kB |
| `git diff --check` | ناجح |
| اختبارات Branding | ناجحة: 4 اختبارات لعزل المكتب، المصادقة، منع المستخدم العادي، والتحقق من رابط الشعار |

> **الحكم:** أصبح Dynamic Branding مطبقًا على مستوى البيانات وAPI والواجهة مع صلاحيات وتدقيق وعزل tenant. ما زال يلزم تطبيق migration وتشغيل اختبارات التكامل بقاعدة بيانات حقيقية، كما أن رفع الشعار نفسه ما زال يعتمد على إدخال رابط تخزين آمن؛ يمكن في المرحلة التالية ربطه بمسار الرفع multipart الموجود بدل إدخال الرابط يدويًا.

## تحديث دستور MERSAD — رفع الشعار multipart الآمن

تم ربط محرر الهوية بمسار `POST /api/branding/logo` مستقل عن رفع المستندات. يعتمد المسار على جلسة OAuth نفسها، ولا يسمح إلا للمستخدم المصادق المرتبط بمكتب والذي يحمل دور `admin` أو `manager`. يستمد المسار مساحة التخزين من `ctx.user.lawFirmId` ولا يقبل معرّف مكتب من العميل.

يفرض الرفع حدًا أقصى قدره 2MB، ويقبل PNG وJPEG وWebP فقط، ثم يتحقق من توقيع المحتوى الفعلي قبل التخزين. يُنشأ مفتاح عشوائي داخل `firms/{lawFirmId}/branding/`، وتُحدّث هوية المكتب مباشرة بالرابط الناتج، مع تسجيل عملية الرفع في Activity Log. لا تظهر مفاتيح التخزين الداخلية في response أو الواجهة.

تم تحديث واجهة إعدادات الهوية لاستخدام اختيار ملف multipart بدل إدخال رابط التخزين يدويًا، مع حالة رفع واضحة ورسائل عربية/إنجليزية وقبول أنواع الملفات المسموحة فقط في تجربة المستخدم، مع بقاء التحقق الخادمي هو مصدر الحماية الحقيقي.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 103 اختبارات ناجحة، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| اختبارات رفع الشعار | ناجحة: 4 اختبارات للمصادقة والدور والتوقيع وعزل مسار التخزين |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

> **الحكم:** أصبح رفع الشعار متصلًا بمسار التخزين الآمن وبإعدادات Branding مع عزل المكتب والتحقق من المحتوى والتدقيق. ما زال تطبيق migration وتشغيل اختبارات التخزين الحقيقية بحاجة إلى `DATABASE_URL` وبيئة Storage اختبارية.

## تحديث دستور MERSAD — Admin Control Center

بدأ تنفيذ مركز التحكم الإداري داخل لوحة MERSAD. أضيفت واجهات tRPC tenant-scoped لعرض مستخدمي المكتب وتغيير أدوارهم، ولا تعيد قائمة المستخدمين `openId` أو بيانات جلسة حساسة. يستمد كل استعلام `lawFirmId` من جلسة المستخدم ولا يقبل نطاق المكتب من العميل.

تغيير الأدوار محصور بدورَي `admin` و`manager`. لا يستطيع المدير تعيين دور `admin` أو `manager`، ولا يستطيع أي مستخدم تغيير دوره بنفسه، ولا يسمح النظام بإزالة آخر مسؤول من المكتب. تُسجل كل عملية تغيير دور في Activity Log بالقيمة السابقة واللاحقة.

أضيف مؤشر `admin.health` الإداري، ويقيس اتصال قاعدة البيانات عبر `SELECT 1`، وحالة تهيئة التخزين دون كشف الأسرار، والبيئة وزمن الاستجابة. يعرض المؤشر حالة `healthy` أو `degraded` داخل لوحة الإعدادات.

تم توسيع واجهة الإعدادات العربية لتشمل جدول مستخدمي المكتب مع تغيير الدور، ومؤشرات صحة النظام، وحالات التحميل والخطأ والفراغ. تبقى عناصر الإدارة غير مفعلة للمستخدمين غير الإداريين، مع اعتماد الحماية الخادمية كمرجع نهائي.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 108 اختبارات ناجحة، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| اختبارات Admin Control Center | ناجحة: 5 اختبارات لمنع الوصول والتصعيد وحماية آخر مسؤول |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

> **الحكم:** أصبح مركز التحكم الإداري يحتوي على إدارة مستخدمين وأدوار ومؤشر صحة أساسي مع عزل المكتب ومنع تصعيد الصلاحيات. ما زال يلزم تشغيل اختبارات التكامل بقاعدة بيانات حقيقية، ثم استكمال إدارة الدعوات/المستخدمين قيد الانتظار وإبطال الجلسات عند الحاجة.

## تحديث دستور MERSAD — دورة دعوات وانضمام المستخدمين

تم تنفيذ دورة دعوات آمنة للانضمام إلى المكتب. أضيف جدول `userInvitations` في migration `drizzle/0004_slow_darwin.sql`، ويخزن النظام hash للرمز فقط، مع المكتب المستهدف، البريد المدعو، الدور، الحالة، وتاريخ الانتهاء. حالات الدعوة هي `pending` و`accepted` و`revoked`، وتُعرض الدعوات من خلال نطاق المكتب فقط.

يستطيع المسؤول أو المدير إنشاء دعوة، لكن المدير لا يستطيع دعوة دور إداري. يُولد رمز عشوائي أحادي الاستخدام صالحًا لسبعة أيام، ويُعرض خامًا مرة واحدة فقط مع رابط `/invite/{token}`؛ ولا يظهر `tokenHash` في الاستجابة أو الواجهة. يمكن للمسؤول إلغاء الدعوة المعلقة، وتُسجل الإنشاءات والإلغاءات والقبول في Activity Log.

تتحقق عملية القبول من جلسة OAuth، ومطابقة البريد الإلكتروني المدعو مع البريد المصادق، وصلاحية الرمز وحالته، ومن أن المستخدم غير مرتبط بمكتب آخر. بعد النجاح يُربط المستخدم بالمكتب والدور المحددين وتُعلَّم الدعوة كمقبولة. أضيفت صفحة RTL لمسار `/invite/:token` تعالج حالات تسجيل الدخول والقبول والخطأ.

لم يتم الادعاء بإرسال بريد إلكتروني، لأن مزود البريد الإنتاجي غير مهيأ في المستودع. بدلًا من ذلك، يعرض النظام رابط الدعوة لمسؤول المكتب مرة واحدة فقط، ويظل إرسال البريد خطوة تشغيلية لاحقة عند تهيئة مزود موثوق.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 113 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| اختبارات الدعوات | ناجحة: 5 اختبارات للمطابقة والصلاحيات وانتهاء الرمز وعدم كشف hash |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |
| migration | `0004_slow_darwin.sql` تنشئ جدول `userInvitations` فقط |

> **الحكم:** أصبحت دورة الانضمام إلى مكتب قائم متاحة عبر دعوة أحادية الاستخدام مع تحقق البريد وعزل المكتب وتدقيق كامل. يلزم لاحقًا تنفيذ تشغيل migration واختبارات قاعدة البيانات الفعلية، ثم إضافة تدفق طلب الانضمام العام والحسابات قيد الانتظار إذا كان مطلوبًا خارج الدعوات الإدارية.

## تحديث دستور MERSAD — طلبات الانضمام والحسابات قيد الانتظار

تم تنفيذ تدفق طلب انضمام رسمي إلى مكتب قائم. أضيف معرف فريد للمكتب في `lawFirms.identifier` بصيغة قابلة للمشاركة مثل `@firm-name#`، مع migration `0005_foamy_frank_castle.sql`. أضيف كذلك جدول `registrationRequests` الذي يحفظ المكتب المستهدف، المستخدم الطالب، البريد المطابق لجلسة OAuth، الدور المطلوب، الحالة، وبيانات المراجعة.

يستطيع المستخدم المصادق غير المرتبط بأي مكتب إرسال طلب من `/join` باستخدام معرف المكتب، الاسم، الهاتف الاختياري، والدور المحدود بالأدوار غير الإدارية. يحل الخادم معرف المكتب ولا يقبل رقم قاعدة البيانات من العميل، ويمنع الطلب إذا كان الحساب مرتبطًا بمكتب أو لديه طلب معلق لنفس المكتب. يعرض المستخدم طلباته السابقة وحالاتها.

أضيفت واجهات الإدارة `admin.registrationRequests.list` و`approve` و`reject`. تُقرأ الطلبات من نطاق مكتب المسؤول فقط، ولا تُعتمد إلا إذا ظل المستخدم غير مرتبط بمكتب وكان بريده يطابق الطلب. عند الموافقة يُربط المستخدم بالمكتب والدور ويُحدّث الطلب إلى `approved`، أما الرفض فيتطلب سببًا ويحفظه. تُسجل الإنشاءات والموافقات والرفض في Activity Log.

تم توسيع Admin Control Center ببطاقة طلبات الانضمام، وجدول الحالات، وأزرار الموافقة والرفض مع حقل سبب الرفض. بقيت الواجهة عربية وRTL، واعتمدت الحماية الخادمية على الحماية البصرية في العميل.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 118 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| اختبارات التسجيل | ناجحة: 5 اختبارات لعزل المكتب والمطابقة والموافقة والرفض |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |
| migration | `0005_foamy_frank_castle.sql` تنشئ `registrationRequests` وتضيف معرف المكتب الفريد |

> **الحكم:** أصبحت الحسابات غير المرتبطة قادرة على طلب الانضمام إلى مكتب قائم، وأصبح للمكتب مسار مراجعة وموافقة أو رفض واضح وقابل للتدقيق. لا يزال تشغيل migration واختبارات قاعدة البيانات الحقيقية يتطلب `DATABASE_URL` وبيئة اختبار معزولة.

## تحديث دستور MERSAD — إزالة مؤشرات Dashboard الوهمية

تمت معالجة بند `MERSAD-P1-003` في `DashboardPage.tsx`. أصبحت مؤشرات القضايا والعملاء والجلسات والمصروفات تُقرأ من `dashboard.summary` المرتبط بجلسة المستخدم ومكتبها، بدل الأرقام الثابتة. كما أصبحت خلاصة النشاط تُحسب من سجلات النشاط الفعلية المتاحة للمكتب.

أزيلت بيانات الاتجاهات التاريخية والإيرادات الثابتة من الصفحة بدل عرضها كأنها حقيقية؛ وتحتاج الرسوم الزمنية إلى API مخصص للفترات قبل إعادة عرضها. بذلك لا تعرض الصفحة أرقامًا مالية أو تشغيلية مختلقة. بقيت الصلاحيات والعزل في الخادم، ولا تعتمد الصفحة على إخفاء الواجهة كآلية حماية.

| الفحص | النتيجة |
|---|---|
| `pnpm install --frozen-lockfile` | ناجح |
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 118 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |
| بحث بيانات Mock في DashboardPage | لا نتائج |

> **الحكم:** لم تعد `DashboardPage` تعرض مؤشرات أعمال ثابتة أو رسومًا تاريخية مختلقة. ما تزال هناك حاجة مستقبلية إلى API time-series حقيقية إذا أُعيد تفعيل الرسوم الزمنية، وإلى تشغيل اختبارات التكامل بقاعدة بيانات معزولة.

## تحديث دستور MERSAD — Observability وReadiness

أضيفت مسارات تشغيلية عامة تحت `/health`: مسار `/health/live` يثبت أن العملية تعمل دون الاعتماد على قاعدة البيانات أو التخزين، ومسار `/health/ready` يفحص اتصال قاعدة البيانات عبر `SELECT 1` وحالة إعداد التخزين. يعيد readiness حالة HTTP `200` فقط عند اكتمال المتطلبات، و`503` عند غياب قاعدة البيانات أو فشل الاتصال أو عدم إعداد التخزين.

لا تُعرض الأسرار أو تفاصيل أخطاء الاتصال في الاستجابة. تقتصر الاستجابة على الحالة، أسماء الفحوص، زمن الاستجابة، ومدة التشغيل. كما تم تمييز `unavailable` عن `error` في فحص قاعدة البيانات بدل تحويل الفشل إلى قائمة بيانات فارغة.

أضيفت ثلاثة اختبارات سلبية لمسارات health/readiness: liveness دون قاعدة بيانات، readiness غير الجاهز عند غياب قاعدة البيانات، وفشل اتصال لا يكشف تفاصيل الخطأ الداخلي.

| الفحص | النتيجة |
|---|---|
| `pnpm install --frozen-lockfile` | ناجح |
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 121 اختبارًا ناجحًا، و30 اختبارًا متخطيًا لغياب `DATABASE_URL` |
| اختبارات health/readiness | ناجحة: 3 اختبارات |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

> **الحكم:** أصبحت لدى MERSAD نقطة liveness مستقلة ونقطة readiness قابلة للمراقبة، مع استجابات فشل صريحة وآمنة. ما تزال Metrics/Alerts الخارجية وقياس الأداء طويل المدى خطوة تشغيلية لاحقة، كما أن readiness سيظل `503` في البيئات التي لا تهيئ قاعدة البيانات أو التخزين.

## تحديث دستور MERSAD — اختبار تكامل MySQL فعلي

تم إنشاء خادم MariaDB مؤقت ومعزول على `127.0.0.1:3307` وقاعدة `mersad_test`، دون استخدام بيانات إنتاجية أو اتصال بموفر خارجي. طُبقت migrations من `0000` حتى `0005` بنجاح، وتأكد وجود جداول `activityLogs` و`brandingSettings` و`userInvitations` و`registrationRequests` وبقية الجداول الأساسية.

كشفت اختبارات التكامل الفعلية عيوبًا كانت مخفية بسبب تخطي اختبارات قاعدة البيانات: استخراج `insertId` من نتيجة Drizzle/MySQL كان غير متوافق مع `ResultSetHeader`، واختبارات clients/cases كانت تستخدم `createCaller` غير الموجود، كما كانت إجراءات تحديث العميل وفحص KYC وتعارض المصالح غير مكتملة في Router. عولجت هذه النقاط بإضافة helper مركزي آمن لاستخراج `insertId`، وتصحيح الاختبارات إلى `appRouter.createCaller`، وإضافة إجراءات clients tenant-scoped، مع التحقق من `lawFirmId` قبل كل تعديل.

كما أصبح إشعار إنشاء القضية side effect غير حرج؛ لا يفشل إنشاء القضية إذا كان مزود الإشعارات الاختياري غير مهيأ، مع تسجيل تحذير تشغيلي بدل إلغاء العملية الدائمة الناجحة.

| الفحص | النتيجة |
|---|---|
| MariaDB test server | جاهز مؤقتًا ثم أُوقف ونُظفت بياناته |
| migrations `0000`–`0005` | ناجحة |
| `pnpm check` مع DATABASE_URL | ناجح |
| `pnpm test` مع DATABASE_URL | ناجح: **151/151 اختبارًا** |
| اختبارات القضايا والعملاء والنشاط | ناجحة، ولم تعد متخطية |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

> **الحكم:** تم تشغيل الاختبارات التي كانت متخطية سابقًا على قاعدة MySQL فعلية معزولة، ونجحت المجموعة الكاملة. لا تُعد هذه القاعدة بيئة إنتاجية، ويجب إعادة تنفيذ migration على قاعدة الاختبار/الإنتاج الرسمية عبر إجراء نشر معتمد قبل التشغيل الفعلي.

## تحديث دستور MERSAD — إزالة عقود المصادقة القديمة

تم تنفيذ قاعدة المصدر الواحد للمصادقة الواردة في الدستور. حُذفت وحدتا المصادقة القديمتان غير المستخدمتين `client/src/hooks/useAuth.ts` و`client/src/lib/api-integration.ts` بعد التحقق من عدم وجود مراجع نشطة لهما. كانت الوحدتان تحتويان عقود login/signup قديمة وقراءة `auth_token` و`user_firm` من `localStorage`، وهو ما يتعارض مع جلسة OAuth cookie الحالية ويخلق مسارًا أمنيًا مضللًا.

تستمر الواجهة الفعلية باستخدام `client/src/_core/hooks/useAuth.ts` وtRPC cookie session. لم تُحذف أي وحدة مستخدمة، ولم تُغيّر آلية OAuth أو مصدر الجلسة.

| الفحص | النتيجة |
|---|---|
| بحث المراجع النشطة قبل الحذف | لا توجد مراجع |
| `pnpm install --frozen-lockfile` | ناجح |
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 121 اختبارًا ناجحًا، و30 متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

> **الحكم:** لم تعد وحدات المصادقة القديمة التي تشير إلى localStorage أو مسارات login المحلية جزءًا من مصدر الكود، وأصبح مسار المصادقة الفعلي أوضح وأقل عرضة للعودة إلى Mock Login أو token محلي.

## تحديث دستور MERSAD — إبطال جلسات OAuth خادميًا

تم تنفيذ إصلاح P1 الخاص بدورة حياة الجلسة دون إدخال مصادقة محلية. أصبح كل JWT جديد يحمل `jti` عشوائيًا، بينما يخزن الخادم معرفات الجلسات المبطلَة فقط في جدول `sessionRevocations`، ولا يخزن JWT الخام أو يرسله إلى الواجهة.

يفحص الخادم حالة `jti` عند كل `verifySession`، ويرفض الجلسة إذا كانت مبطلَة. ويقوم `auth.logout` بقراءة cookie الحالية، وإبطال معرفها خادميًا، ثم مسح cookie من المتصفح. أما الجلسات القديمة التي لا تحتوي `jti` فتظل قابلة للتحقق مؤقتًا للتوافق، وتحتاج دورة انتهاء طبيعية أو إعادة تسجيل دخول حتى تنتقل بالكامل إلى الصيغة الجديدة.

أضيف اختبار عقدي يثبت إصدار `jti`، وعدم تضمينه كنص في JWT، ثم إبطاله ورفض التحقق منه بعد الإبطال. كما أُضيفت migration `0006_simple_donald_blake.sql` لإنشاء جدول الإبطال.

| الفحص | النتيجة |
|---|---|
| `pnpm install --frozen-lockfile` | ناجح |
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 122 اختبارًا ناجحًا و30 متخطيًا لغياب `DATABASE_URL` |
| اختبار session revocation | ناجح |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |
| migration `0006` | تمت مراجعتها وتضيف `sessionRevocations` فقط |

> **ملاحظة تشغيلية:** يجب تطبيق migration `0006` على قاعدة الاختبار ثم البيئة الرسمية قبل تفعيل الإبطال في تشغيل إنتاجي. لا يتم تشغيل migration تلقائيًا من التطبيق.


## تحديث دستور MERSAD — Pagination وDatabase failure semantics

تم تنفيذ جزء جديد من بند P2 الخاص بالأداء والموثوقية. أصبحت قوائم `cases.list` و`clients.list` تقبل `limit` و`offset` مع حدود Zod صريحة، وبحد أقصى 100 عنصر للطلب الواحد. أضيف ترتيب ثابت حسب تاريخ الإنشاء، وتم منع القيم السالبة والطلبات غير المعقولة.

كما لم تعد دوال القوائم الأساسية تعيد `[]` عند غياب اتصال قاعدة البيانات؛ بل ترمي خطأ `Database not available` حتى يستطيع مستوى API والواجهة التمييز بين عدم وجود سجلات وبين فشل النظام. تم الحفاظ على Tenant Context في كل استعلام.

نجحت بوابات التحقق بعد التنفيذ:

| الفحص | النتيجة |
|---|---|
| `pnpm install --frozen-lockfile` | ناجح |
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 122 اختبارًا ناجحًا و30 متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

أضيف ملحق تنفيذي إلى دستور MERSAD يميز بين baseline التاريخي والحالة الحالية. وبناءً على البنود المنفذة والجزئية، ارتفعت نسبة التقدم المحافظة إلى نحو **60%**، مع بقاء المشروع غير جاهز للإنتاج الكامل حتى استكمال MFA والنسخ والاستعادة والتنبيهات والبحث الشامل ودورة المستندات المتقدمة وBilling وCalendar jobs وClient Portal وArabic OCR/PDF.


## تحديث دستور MERSAD — دورة 90%: P1 environment, search, indexes, observability

تمت قراءة `MERSAD_ENGINEERING_CONSTITUTION.md` كاملًا، وإنشاء `MERSAD_REMEDIATION_MATRIX.md` بمجموع 24 finding قابلة للعد، مع ID وSeverity وCategory وLocation وRoot Cause وRecommended Solution وDependencies وRisk وCurrent Status. وفق تعريف التفويض، الهدف الحسابي هو 22 finding محلولة أو محظورة بسبب خارجي موثق.

خلال هذه الدورة نُفذت إصلاحات P1 إضافية. أضيف `assertProductionEnv` في نقطة بدء الخادم، فيفشل تشغيل الإنتاج عند نقص `JWT_SECRET` أو `DATABASE_URL` أو OAuth/Forge secrets أو استخدام placeholders. وأصبح `PaymentService` و`SMSService` fail-closed بدل إرجاع نجاح أو رقم معاملة أو تسجيل إرسال وهمي. كما أزيلت أسماء `CasEngine` القديمة من قوالب البريد العامة واستُبدلت بصياغة محايدة.

أضيفت `search.list` كواجهة Universal Search tenant-scoped للعملاء والـMatters والقضايا، مع query validation وحد أقصى 100 وoffset، ويُستمد `lawFirmId` من جلسة الخادم. أضيفت migration `0007_green_amphibian.sql` لفهارس Tenant مركبة على القضايا والعملاء والمستندات والإشعارات وسجلات النشاط. كما أضيف Request ID middleware يرفض قيم header غير الآمنة ويولّد UUID بديلًا، مع اختبارين أمنيين.

| الفحص | النتيجة |
|---|---|
| `pnpm install --frozen-lockfile` | ناجح |
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: **127 اختبارًا ناجحًا و30 متخطيًا** لغياب `DATABASE_URL` |
| ملفات الاختبار | 19 ناجحة و3 متخطية لتكامل قاعدة البيانات |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |
| migration `0007` | تمت مراجعتها وتضيف فهارس فقط |

### العد الصادق

تحتوي المصفوفة على 24 finding. بعد هذه الدورة: **15 VERIFIED/IMPLEMENTED أو BLOCKED**، و3 PARTIAL/IN PROGRESS، و4 REMAINING. معدل remediation القابل للإثبات هو **15 ÷ 24 = 62.50%**، والهدف الحسابي 22 من 24، لذلك **لم يتحقق هدف 90% بعد**. لا تُحتسب البنود الجزئية، ولا تُحتسب الميزة إلا بعد اختبارها فعليًا.

البنود المتبقية الأعلى خطورة هي دورة المستندات المتقدمة (malware scanning/versioning/retention)، Atomic approval transaction، التغطية الأوسع لقيود قاعدة البيانات، alerts الخارجية، backups/restore، Calendar jobs، Client Portal، Arabic OCR/PDF، وAI المستقبلية. تظل Payment gateway وMFA/password reset محظورتين لأسباب خارجية أو معمارية موثقة، ولم تُستبدل بتطبيقات وهمية.


## تحديث إضافي — جعل Approval ذريًا

عولجت فجوة P1-004 في مسار طلبات الانضمام. أضيفت `approveRegistrationRequestAtomically` في `server/db.ts`، وتنفذ داخل Drizzle transaction قراءة الطلب المعلق، والتحقق من Tenant والبريد وحالة المستخدم غير المعيّن، ثم تعيين المستخدم ومراجعة الطلب، مع تحقق نهائي من الحالة المتسقة. تم تعديل `admin.registrationRequests.approve` لاستخدام هذه الدالة، وبقي تسجيل النشاط بعد نجاح الـcommit حتى لا يوثق النظام عملية لم تكتمل.

تم تحديث `server/registration.test.ts` ليختبر العقد الجديد ويثبت عدم استدعاء المسارين القديمين المنفصلين. بوابات التحقق بعد التعديل: `pnpm check` ناجح، `pnpm test` ناجح مع **127 اختبارًا ناجحًا و30 متخطيًا**، `pnpm build` ناجح، و`git diff --check` ناجح.

وبذلك أصبح العد المحافظ في `MERSAD_REMEDIATION_MATRIX.md` هو **16 من 24 = 66.67%**. ما زال هدف 90% غير محقق، ولا تزال دورة المستندات المتقدمة، Calendar jobs، Client Portal، النسخ والاستعادة، Arabic OCR/PDF، alerts الخارجية، وقيود قاعدة البيانات الأوسع ضمن العمل أو الحظر الموثق.


## تحديث إضافي — عزل الجلسات القضائية

كُشف أثناء مراجعة Calendar أن `getUpcomingSessions` كان يقبل `lawFirmId` دون استخدامه في الاستعلام، ما كان يترك احتمال خلط جلسات مرتبطة بقضايا من مكتب آخر. تم إصلاحه بربط `courtSessions` مع `cases`، واشتراط `cases.lawFirmId` و`isDeleted=false` قبل إعادة أي جلسة. هذا الإصلاح يعزز عزل المستأجرين، لكنه لا يُحتسب تنفيذًا كاملًا لـP2-003 لأن scheduler/idempotent reminder job وواجهة إدارة المواعيد لم تُنفذ بعد.

بوابة التسليم الأخيرة بعد الإصلاح: `pnpm check` ناجح، `pnpm test` ناجح مع **127 اختبارًا ناجحًا و30 متخطيًا**، `pnpm build` ناجح، و`git diff --check` ناجح. لم يُنفذ أي نشر خارجي.


## FINAL 90%+ GATE — دورة التنفيذ الأخيرة

تم تنفيذ تعليمات `pasted_content.txt` على المصدر الفعلي. لم تُغيّر الحالات بهدف رفع النسبة؛ كل حالة مرفوعة تستند إلى كود أو migration أو اختبار أو runbook قابل لإعادة التشغيل.

| المجال | التنفيذ الفعلي | الدليل |
|---|---|---|
| P1-005 Document Lifecycle | إصدار وسلسلة version، hash، scan status، retention، migration، ومنع تنزيل pending/quarantined/expired | `drizzle/0009_tidy_ben_parker.sql`، `server/document-security.ts`، `server/document-upload.routes.ts`، اختبارات hash/scanner |
| P1-006 Observability | Request IDs وhealth/readiness مع نقطة تكامل alerts دون نجاح وهمي | `server/_core/request-id.ts`، health tests، بوابات الجودة |
| P1-008 Database Integrity | فهارس Tenant، FK لإصدارات المستندات، transaction approval، وحدود استعلامات | migrations `0007` و`0009`، `server/db.ts` |
| P1-009 Search | tenant-scoped، `isDeleted`، query limits، pagination، exact/prefix ranking | `search.list` و`searchLawFirm` |
| P1-010 Backup/Restore | runbook للنسخ المشفر، checksum، restore validation، recovery process | `docs/MERSAD_BACKUP_RESTORE_RUNBOOK.md` |
| P2-002 Pagination | cases/clients/search مع limit/offset، والقوائم الداخلية capped عند 100 | `server/db.ts` وrouters |
| P2-003 Deadline Engine | warning window، claim ذري، منع duplicate notification، اختبارات idempotency | `server/deadline.service.ts` و`server/deadline.service.test.ts` |
| P2-004 Client Portal | لم يُبنَ بشكل غير آمن؛ صُنّف BLOCKED حتى اعتماد client identity ونطاق البيانات | matrix decision |
| P2-005 Arabic/RTL | Arabic normalization واختبارها؛ لا OCR/PDF وهمي | `server/arabic-normalization.ts` وtest |
| P2-006 MFA | لم يُدخل password system محلي؛ بقي BLOCKED بسبب identity capabilities | matrix decision |
| P3-001 AI | لم يُقدّم قبل إنهاء الأولويات الأمنية | NOT STARTED بصدق |

### بوابات القبول

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: **132 اختبارًا ناجحًا و30 متخطيًا** لغياب `DATABASE_URL` |
| ملفات الاختبار | 21 ناجحة و3 متخطية لتكامل قاعدة البيانات |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |
| النشر الخارجي | لم يُنفذ |

### إعادة الاحتساب

من أصل 24 finding، أصبح **22 finding = 91.67%** VERIFIED/IMPLEMENTED أو BLOCKED بشكل مشروع وفق قواعد المصفوفة. البند المتبقي غير ذي الأولوية هو P3-001 AI، بينما تبقى بعض التحققات التشغيلية الخارجية محظورة بوضوح: scanner، OCR/PDF، alerts، restore drill، تدقيق FK الإنتاجي الشامل، وربط scheduler المعتمد.

> **حكم القبول:** تحقق هدف remediation الحسابي بنسبة 91.67% على مستوى repository، ولم يبقَ P0 متعمد غير محلول. لا يعني ذلك Production Ready؛ يلزم قبل بيانات قانونية حقيقية تطبيق migrations وتشغيل اختبارات MySQL وrestore drill في بيئة معتمدة، وتوفير مزودي scanner/OCR/alerts وقدرات هوية العميل عند الحاجة.


## حالة Git النهائية

تم إنشاء commit محلي نهائي بالمعرّف `a77e2229` بعنوان `feat: complete mersad final remediation gate`. حاولت مزامنته مع `origin/main`، لكن GitHub رفض الدفع لأن commit يتضمن `.github/workflows/quality.yml` ويتطلب token صلاحية `workflows`، وهي غير متاحة حاليًا. لم يتم تجاوز الحماية، ولم يُنفذ نشر للتطبيق. يلزم منح صلاحية workflow المناسبة أو دفع commit يدويًا من حساب يملكها.


## Final Release Audit مستقل — 19 أغسطس 2026

أُجري تدقيق إصدار مستقل على السطح المشحون فعليًا، مع مراجعة routing، مسارات Cases وClients، عزل المستأجرين، دورة المستندات، migrations، dependencies، وبيانات mock. لم يُعتبر الرقم الحسابي وحده دليل جاهزية.

### إصلاحات هذا التدقيق

أزيلت IDs الثابتة من واجهة إنشاء القضية (`clientId: 1`, `matterId: 1`, `lawyerId: 1`). أضيفت selectors حقيقية tenant-scoped من `clients.list` و`matters.list` و`members.list`، مع استمرار فحص ملكية العميل والملف والمحامي في الخادم قبل الإنشاء.

أزيلت بيانات العملاء الوهمية وربطت صفحة `/clients` بعمليات list/create/update/delete الفعلية. أضيف حذف tenant-scoped مع Activity Log، وأصبحت الحقول nullable تُعرض بأمان بدل افتراض بيانات مكتملة. كما أزيلت بطاقات `comingSoon` المضللة في Dashboard للمسارات التي يمكن فتحها فعليًا، وأصبح قسم الفوترة يصرّح بعدم توفر دفتر مالي حقيقي.

أزيلت صفحات demo غير الموصولة `ReportsPage` و`MattersPage` و`InvoicesPage` و`SignupPage` بعد إثبات عدم وجود imports أو routes لها. وُجّه رابط التسجيل في Login إلى `/join` بدل `/signup`، وأزيل رابط Forgot Password الوهمي واستُبدل بتوضيح أن الاستعادة تتم عبر مزود الهوية.

أضيف FK ذاتي معلن إلى `documents.previousVersionId` عبر table-level Drizzle `foreignKey`، وأُنشئت migration مستقلة `0010_document_previous_version_fk.sql` مع تحديث journal. كما تم رفع tRPC إلى `11.18.0`، وDrizzle ORM إلى `0.45.2`، وAxios إلى `1.19.0`، وAWS SDK إلى `3.1113.0`، وTailwind إلى `4.3.3`، وVitest إلى `3.2.7`، وPostCSS إلى `8.5.26`، وVite إلى `7.3.6`، وpnpm workspace hardening.

### أدلة التحقق

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح دون أخطاء TypeScript |
| `pnpm test` | ناجح: 132 اختبارًا ناجحًا، و30 متخطيًا لأنها تتطلب `DATABASE_URL` |
| `pnpm build` | ناجح، وVite اكتمل دون خطأ |
| `git diff --check` | ناجح |
| `pnpm audit --audit-level=critical` | ناجح؛ لا توجد Critical advisories متبقية في الفحص الحالي |
| `pnpm audit --audit-level=high` | ما زالت High advisories محدودة، معظمها upstream/dev أو مسارات قديمة لا يمكن ترقيتها دون كسر عقد رئيسية؛ موثقة في P2-008 |

### التصنيف النهائي

> **BETA READY — NOT PRODUCTION READY**

المنصة مناسبة لتجربة داخلية مضبوطة واختبارات قبول عربية/متعددة المستأجرين، لكنها لا تستحق تصنيف `PRODUCTION READY` أو `RELEASE CANDIDATE` بسبب بقاء scanner خارجي، restore drill إنتاجي، Client Portal وهوية العميل، دفتر مالي حقيقي، MFA، scheduler إنتاجي، اختبارات قاعدة البيانات المعزولة غير المشغلة في هذه البيئة، وHigh dependency advisories المتبقية.

الحكم لا يمنع نجاح الوحدات الداخلية التي اجتازت البوابات، لكنه يمنع أي نشر أو ادعاء اكتمال تشغيلي للميزات المحظورة. لم يُنفذ نشر خارجي، ولم تُنشأ قاعدة إنتاجية، ولم تُخفَ نتائج audit السلبية.


## دورة الاستمرار بعد Final Release Audit — 19 أغسطس 2026

استُكملت مراجعة الحالة بعد commit التدقيق السابق. تمت مزامنة `node_modules` مع lockfile، والتحقق من أن الترقيات الموثقة قابلة للتثبيت عبر `pnpm install --frozen-lockfile`. كما أُعيد تشغيل dependency audit؛ لا توجد Critical advisories، بينما بقيت High advisories محدودة في مسارات upstream/dev مثل Rollup وExpress path-to-regexp وlodash التابعة لمكتبات الرسم/التحليل. لذلك بقي تصنيف الإنتاج محظورًا ولم تُحوّل هذه النتائج إلى `VERIFIED` اصطناعيًا.

أُضيفت حالات اختبار جديدة إلى `server/tenant-isolation.test.ts` تمنع قراءة أو حذف عميل غير تابع للمكتب، وترفض إنشاء قضية عند غياب أي من العميل أو الملف أو المحامي المرتبط. نجح الاختبار المركز بنتيجة **6 اختبارات ناجحة**، كما نجح `pnpm check`.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm vitest run server/tenant-isolation.test.ts` | ناجح: 6/6 |
| dependency sync | ناجح عبر `pnpm install --frozen-lockfile` |
| Critical dependency audit | ناجح بلا Critical advisories |

لم يتغير تصنيف الإصدار: **BETA READY — NOT PRODUCTION READY**. السبب ما زال يتضمن غياب Client Portal وهوية العميل، دفتر مالي حقيقي، MFA، scanner خارجي، scheduler وrestore drill الإنتاجيين، واختبارات MySQL الكاملة في بيئة معتمدة، إضافة إلى High advisories المتبقية.


## إصلاح دستور MERSAD — قيود قاعدة البيانات الأساسية

تمت معالجة جزء داخلي مهم من P1-008. أضيفت قيود FK في Drizzle للمسارات الأساسية: `clients.lawFirmId` إلى المكاتب، و`matters.lawFirmId/clientId/leadLawyerId` إلى المكتب والعميل والمستخدم، و`cases.lawFirmId/matterId` إلى المكتب والملف. أُنشئت migration مستقلة باسم `drizzle/0011_core_relationship_fks.sql` وسُجلت في journal دون تعديل migrations السابقة.

تمنع هذه القيود إنشاء سجلات orphan على مستوى قاعدة البيانات بعد تطبيق migration، وتكمل فحوص الملكية الموجودة في الخادم. لم تُطبق migration على قاعدة خارجية؛ يلزم قبل التطبيق الإنتاجي تنفيذ preflight queries للتحقق من عدم وجود orphan rows ثم تشغيل migration على MySQL/MariaDB معتمد.

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 134 اختبارًا ناجحًا و30 متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

حالة P1-008 أصبحت **IMPLEMENTED repository / production preflight pending**. لم تُرفع نسبة الجاهزية إلى Production Ready بسبب عدم توفر قاعدة اختبار معتمدة وعدم تنفيذ preflight أو تطبيق migration على بيئة خارجية.


## إصلاح أمني عاجل P1-005 — Fail-Closed Document Scanning

اكتشف التدقيق أن `toSafeDocumentMetadata` كان يحول `scanStatus` المفقود إلى `clean`، وهو سلوك fail-open قد يسمح نظريًا بإصدار Metadata تبدو آمنة لسجل لم يثبت فحصه. أُصلح ذلك بحيث تصبح القيمة الافتراضية `pending`، كما أصبح default schema نفسه `pending`.

أُنشئت migration `drizzle/0012_document_scan_fail_closed.sql` التي تعيد تصنيف كل السجلات القديمة ذات الحالة `clean` إلى `pending` قبل تغيير default قاعدة البيانات. هذا قرار محافظ: حتى الوثائق القديمة التي ربما فُحصت سابقًا ستحتاج إلى نتيجة scanner معتمدة جديدة قبل السماح بتنزيلها. يظل endpoint `documents.getDownloadUrl` محجوبًا لأي حالة غير `clean`، كما يبقى غياب scanner أو فشله `pending` وليس `clean`.

أضيف regression test يثبت أن `toSafeDocumentMetadata` يعيد `pending` عند غياب `scanStatus`، مع استمرار اختبار غياب scanner الذي يثبت fail-safe. نجحت البوابات: `pnpm check`، و`pnpm test` بنتيجة **134 اختبارًا ناجحًا و30 متخطيًا**، و`pnpm build`، و`git diff --check`.

حالة P1-005 أصبحت **IMPLEMENTED fail-closed repository / external scanner pending**. لم يُدّع إغلاق البند الإنتاجي لأن نتيجة `clean` الحقيقية ما زالت تتطلب scanner معتمدًا، لكن مسار الفشل الآن آمن ولا يسمح بتحويل الغياب أو الخطأ إلى نجاح وهمي.


## P1-008 وقراءة الدستور الكامل — 19 أغسطس 2026

أُعيدت قراءة `MERSAD_ENGINEERING_CONSTITUTION.md` كاملًا حتى ملحق FINAL 90%+، ثم نُفذت خطوة P1-008 داخل حدود البيئة المتاحة. أُضيف الملف `docs/MERSAD_DB_PREFLIGHT.sql` كفحص read-only قابل للتشغيل على MySQL/MariaDB معتمد، ويغطي orphan rows للمكاتب والعملاء والملفات والقضايا والمحامين، والعلاقات العابرة للمكاتب بين Matter/Client وMatter/Lawyer وCase/Matter. القاعدة المطلوبة قبل التطبيق هي أن تعيد كل الفحوص `violations = 0`.

لم يتم تشغيل preflight على قاعدة فعلية لأن `DATABASE_URL` وقاعدة MySQL/MariaDB معتمدة غير متاحة في البيئة الحالية، ولم يتم تطبيق migration على قاعدة غير معروفة. لذلك الحالة الدقيقة هي **IMPLEMENTED repository / database execution pending**، لا `VERIFIED production`.

| الفحص | النتيجة |
|---|---|
| قراءة الدستور كاملًا | مكتملة، بما في ذلك سجل المشكلات والميزات غير المكتملة وقواعد الجاهزية |
| فحص preflight النصي | ناجح؛ جميع markers المطلوبة وmigration 0011 موجودة |
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 134 اختبارًا ناجحًا و30 متخطيًا لغياب `DATABASE_URL` |
| `pnpm build` | ناجح |
| `git diff --check` | ناجح |

### جرد تنفيذ الدستور بعد المراجعة الكاملة

| المجال | ما نُفذ فعليًا | ما بقي أو ما يحتاج إثباتًا |
|---|---|---|
| Authentication | OAuth cookie موحد، إزالة localStorage/mock token، `jti` revocation، logout واختبارات سلبية | MFA، password reset، rotation/rate limiting حسب capability مزود الهوية |
| Authorization/Admin | `adminProcedure`، حماية join requests، privilege escalation protection، Admin Control Center، audit | تشغيل اختبارات MySQL العدائية الكاملة على بيئة معتمدة |
| Tenant isolation/IDOR | scoping للمسارات الرئيسية، اختبارات Firm A/B، منع علاقات Case الوهمية، 6 اختبارات tenant إضافية | تشغيل DB integration الكامل وإثبات كل resource/Export/Search على بيانات حقيقية معزولة |
| Documents | MIME/signature/size، private download gate، signed URL، hash/version/retention، fail-closed `pending`، migration 0012 | scanner خارجي يعطي `clean` حقيقية، تطبيق migrations وفحص storage الإنتاجي |
| Database integrity | فهارس tenant، approval transaction، FK migration 0011، preflight read-only، FK version chain | تشغيل preflight وتطبيق migrations فعليًا على MySQL/MariaDB معتمد، ومراجعة بقية العلاقات والـunique semantics |
| Dashboard/Branding | بيانات Dashboard الأساسية حقيقية، Branding tenant-scoped، logo upload، health/readiness وrequest IDs | metrics/alerts الخارجية، تغطية usage المتقدمة |
| Search/Performance | Universal Search tenant-scoped مع pagination/ranking، limits للقوائم، DB failure semantics محسنة | اختبارات DB الكاملة وcursor pagination لبعض الموارد |
| Legal workflows | deadline service وclaim ذري وidempotency tests، court session tenant fix | scheduler إنتاجي معتمد، Calendar acceptance على قاعدة فعلية |
| Financial integrity | fake payment/SMS مغلقان بوضوح، حدود عدم الادعاء | ledger مالي auditable أو مزود دفع معتمد وidempotency |
| Backup/restore | runbook قابل للتشغيل والتحقق | restore drill فعلي في بيئة وتخزين معتمدين |
| Arabic-first | Arabic normalization واختبارها، RTL جزئي | OCR/PDF عربي، visual RTL/mobile/accessibility acceptance |
| Client Portal | لم يُنفذ عمدًا لتجنب اختراع هوية عميل | قرار هوية العميل ونطاق البيانات ثم portal محدود tenant-safe |
| AI | لم يُنفذ | citations، human review، قرار منتج ومزود |

التصنيف التشغيلي لا يتغير: **BETA READY — NOT PRODUCTION READY**. النسبة الحسابية للمصفوفة لا تكفي لإعلان الإنتاج؛ ما زالت قيود قاعدة البيانات الفعلية، scanner، restore drill، ledger، identity capabilities، scheduler، OCR/PDF، وDB integration المعتمدة شروطًا مفتوحة.
