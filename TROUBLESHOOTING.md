# دليل استكشاف الأخطاء - CasEngine\n\nحلول سريعة للمشاكل الشائعة\n\n## 🔧 مشاكل التثبيت\n\n### المشكلة: \"npm ERR! code ERESOLVE\"\n\n**السبب**: تضارب في الاعتماديات\n\n**الحل**:\n```bash\n# استخدم --legacy-peer-deps\nnpm install --legacy-peer-deps\n\n# أو استخدم pnpm\npnpm install\n```\n\n### المشكلة: \"Module not found\"\n\n**السبب**: الاعتماديات غير مثبتة\n\n**الحل**:\n```bash\n# أعد تثبيت الاعتماديات\nrm -rf node_modules pnpm-lock.yaml\npnpm install\n\n# تحقق من package.json\ncat package.json | grep dependencies\n```\n\n## 🗄️ مشاكل قاعدة البيانات\n\n### المشكلة: \"Connection refused\"\n\n**السبب**: قاعدة البيانات غير متشغلة\n\n**الحل**:\n```bash\n# تحقق من حالة PostgreSQL\nsudo systemctl status postgresql\n\n# ابدأ PostgreSQL\nsudo systemctl start postgresql\n\n# تحقق من الاتصال\npsql -U postgres -d law_firm\n```\n\n### المشكلة: \"Database does not exist\"\n\n**السبب**: قاعدة البيانات لم تُنشأ\n\n**الحل**:\n```bash\n# أنشئ قاعدة البيانات\ncreatdb law_firm\n\n# طبق الهجرات\npnpm db:push\n\n# تحقق من الجداول\npsql -U postgres -d law_firm -c \"\\dt\"\n```\n\n### المشكلة: \"Migration failed\"\n\n**السبب**: مشكلة في ملف الهجرة\n\n**الحل**:\n```bash\n# تحقق من حالة الهجرات\npnpm db:status\n\n# أعد تشغيل الهجرات\npnpm db:push --force\n\n# اعرض السجلات\npnpm db:logs\n```\n\n## 🚀 مشاكل التشغيل\n\n### المشكلة: \"Port already in use\"\n\n**السبب**: المنفذ مستخدم بواسطة عملية أخرى\n\n**الحل**:\n```bash\n# ابحث عن العملية\nlsof -i :3000\n\n# أوقف العملية\nkill -9 <PID>\n\n# أو استخدم منفذ مختلف\nPORT=3001 pnpm dev\n```\n\n### المشكلة: \"Cannot find module\"\n\n**السبب**: مسار الاستيراد غير صحيح\n\n**الحل**:\n```bash\n# تحقق من مسار الملف\nls -la client/src/components/\n\n# تحقق من tsconfig.json\ncat tsconfig.json | grep paths\n\n# استخدم المسار الصحيح\nimport { Component } from '@/components/Component';\n```\n\n### المشكلة: \"Vite HMR error\"\n\n**السبب**: مشكلة في Hot Module Replacement\n\n**الحل**:\n```bash\n# امسح الكاش\nrm -rf .vite\n\n# أعد تشغيل الخادم\npnpm dev\n\n# أو استخدم --force\npnpm dev -- --force\n```\n\n## 🔐 مشاكل المصادقة\n\n### المشكلة: \"JWT token expired\"\n\n**السبب**: انتهت صلاحية التوكن\n\n**الحل**:\n```bash\n# امسح الكوكيز\n# في المتصفح: DevTools > Application > Cookies > Delete\n\n# أعد تسجيل الدخول\n# الصفحة ستعيد التوجيه تلقائياً\n```\n\n### المشكلة: \"Unauthorized access\"\n\n**السبب**: المستخدم ليس لديه صلاحيات\n\n**الحل**:\n```bash\n# تحقق من دور المستخدم\nSELECT id, email, role FROM users WHERE id = <user_id>;\n\n# حدّث الدور\nUPDATE users SET role = 'admin' WHERE id = <user_id>;\n```\n\n### المشكلة: \"CORS error\"\n\n**السبب**: مشكلة في Cross-Origin Resource Sharing\n\n**الحل**:\n```typescript\n// تحقق من server/_core/cors.ts\nconst corsOptions = {\n  origin: process.env.FRONTEND_URL,\n  credentials: true\n};\n\napp.use(cors(corsOptions));\n```\n\n## 🎨 مشاكل الواجهة\n\n### المشكلة: \"Styles not loading\"\n\n**السبب**: مشكلة في Tailwind CSS\n\n**الحل**:\n```bash\n# تحقق من tailwind.config.js\ncat tailwind.config.js\n\n# أعد بناء الأنماط\npnpm build:css\n\n# امسح الكاش\nrm -rf .next .vite\n```\n\n### المشكلة: \"Component not rendering\"\n\n**السبب**: خطأ في الكود\n\n**الحل**:\n```bash\n# افتح DevTools\nF12\n\n# اعرض Console\nConsole tab\n\n# ابحث عن الأخطاء\n# تحقق من React DevTools\n\n# استخدم React.StrictMode للكشف عن المشاكل\n```\n\n## 📊 مشاكل الأداء\n\n### المشكلة: \"Slow page load\"\n\n**السبب**: عمليات بطيئة\n\n**الحل**:\n```bash\n# استخدم Lighthouse\npnpm lighthouse https://localhost:3000\n\n# تحقق من حجم Bundle\npnpm build -- --analyze\n\n# حسّن الصور\nconvert image.png -quality 80 image-optimized.png\n```\n\n### المشكلة: \"High memory usage\"\n\n**السبب**: تسرب الذاكرة\n\n**الحل**:\n```bash\n# استخدم Node Inspector\nnode --inspect server.js\n\n# افتح chrome://inspect\n# اعرض Memory profiler\n\n# ابحث عن تسرب الذاكرة\n```\n\n## 🧪 مشاكل الاختبار\n\n### المشكلة: \"Tests failing\"\n\n**السبب**: مشكلة في الاختبار\n\n**الحل**:\n```bash\n# شغّل اختبار واحد\npnpm test -- --testNamePattern=\"specific test\"\n\n# اعرض التفاصيل\npnpm test -- --verbose\n\n# استخدم debug mode\nDEBUG=* pnpm test\n```\n\n### المشكلة: \"Coverage too low\"\n\n**السبب**: تغطية اختبارات غير كافية\n\n**الحل**:\n```bash\n# اعرض تقرير التغطية\npnpm test -- --coverage\n\n# اكتب اختبارات إضافية\n# ركز على الأسطر غير المغطاة\n```\n\n## 🌐 مشاكل النشر\n\n### المشكلة: \"Build failed\"\n\n**السبب**: خطأ في البناء\n\n**الحل**:\n```bash\n# امسح الملفات المؤقتة\nrm -rf dist build .next\n\n# أعد البناء\npnpm build\n\n# اعرض السجلات\npnpm build 2>&1 | tee build.log\n```\n\n### المشكلة: \"Deployment timeout\"\n\n**السبب**: النشر يستغرق وقتاً طويلاً\n\n**الحل**:\n```bash\n# قلل حجم البناء\npnpm build -- --minify\n\n# استخدم caching\n# تحقق من .dockerignore\n\n# استخدم parallel builds\n```\n\n## 📞 الحصول على المساعدة\n\n### إذا لم تجد الحل\n\n1. **ابحث في GitHub Issues**\n   ```\n   https://github.com/ossamaamr/law-firm-management/issues\n   ```\n\n2. **اقرأ السجلات**\n   ```bash\n   tail -f .manus-logs/devserver.log\n   tail -f .manus-logs/browserConsole.log\n   ```\n\n3. **افتح Issue جديد**\n   - اشرح المشكلة بوضوح\n   - أرفق السجلات\n   - أرفق خطوات إعادة الإنتاج\n\n## 🔍 نصائح مفيدة\n\n### تفعيل وضع التصحيح\n```bash\nDEBUG=* pnpm dev\n```\n\n### عرض متغيرات البيئة\n```bash\nenv | grep -E \"DATABASE|JWT|NODE\"\n```\n\n### اختبار الاتصال\n```bash\n# اختبر قاعدة البيانات\npsql -U postgres -d law_firm -c \"SELECT 1\"\n\n# اختبر الخادم\ncurl http://localhost:3000/health\n```\n\n---\n\n**آخر تحديث**: فبراير 2026\n\n**الإصدار**: 1.0.0\n
